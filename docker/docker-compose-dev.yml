version: '2'

services:
  nginx:
    image: nginx:1.13-alpine
    ports:
      - "80:80"
    volumes:
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
      - ./..:/code

  php-fpm:
    build: php-fpm
    volumes:
      - ./..:/code
